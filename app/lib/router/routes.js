Router.route('/', {
  name: 'home',
  controller: 'HomeController',
  action:'action'
});


Router.route('/leaderboard', {
  name: 'leaderboard',
  controller: 'HomeController',
  action:'leaderboard'
});